语法：zip(iterbable1,iterable2,...)

说明：將各个迭代器同一位置的对象打包称一个个元祖，然后返回由这些元组组成的列表
如果各个迭代器元素个数不一致，则返回列表长度与最短的迭代器长度相同。利用*号操作符，可以將元组解压为列表

实例：
>>> a = [1,2,3]
>>> b = [4,5,6]
>>> c = [4,5,6,7,8]
>>> zipped = zip(a,b) #返回[(1, 4), (2, 5), (3, 6)]
>>> zip(a,c) #返回[(1,4),(2,5),(3,6)]
>>> zip(*zipped) *与zip相反，*zipped可理解为解压，返回二维矩阵式
#返回[(1,2,3),(4,5,6)]

